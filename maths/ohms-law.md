# Ohm's law

For a ReRAM read operation we use conductance form:

```text
I = V × G
```

`V` is applied voltage, `G` is programmed conductance, and `I` is measured current. This abstraction is useful for architecture lessons but omits device non-linearity and read-disturb effects.
