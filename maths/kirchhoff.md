# Kirchhoff's current law

At a node, incoming and outgoing currents balance. In a crossbar output line, cell currents add:

```text
I_column = I0 + I1 + ... + In
```

Combining this sum with `Ii = Vi × Gi` produces a dot product.
